# Глава 2: Основы Swift — переменные и типы данных

## Введение

Swift — это современный язык программирования, созданный Apple в 2014 году. Он сочетает в себе мощность и производительность с простотой и безопасностью. В этой главе мы изучим фундаментальные концепции Swift: переменные, константы и типы данных.

Если вы знакомы с JavaScript, многие концепции покажутся знакомыми, но есть важные отличия, которые мы обсудим.

---

## 2.1 Введение в Swift

### Характеристики Swift

- **Безопасный** (Safe) — Swift предотвращает многие ошибки на этапе компиляции
- **Быстрый** (Fast) — производительность сравнима с C++
- **Выразительный** (Expressive) — чистый и читаемый синтаксис
- **Современный** — поддерживает современные парадигмы программирования

### Swift vs JavaScript

| Особенность | JavaScript | Swift |
|-------------|------------|-------|
| Типизация | Динамическая | Статическая |
| Компиляция | Интерпретируемый | Компилируемый |
| Точка с запятой | Опционально | Не нужна |
| Объявление переменных | var, let, const | var, let |
| Null-значение | null, undefined | nil (только для Optional) |

### Ваша первая программа на Swift

Откройте Playground и напишите:

```swift
import UIKit

// Это комментарий — он не выполняется
print("Привет, мир!")
```

`print()` — это функция, которая выводит текст в консоль. Как `console.log()` в JavaScript.

---

## 2.2 Переменные и константы (var и let)

В Swift есть два способа хранить данные:

### Переменные (var)

**Переменная** — это контейнер для данных, значение которого может изменяться.

```swift
var score = 0
print(score)  // 0

score = 10
print(score)  // 10

score = score + 5
print(score)  // 15
```

### Константы (let)

**Константа** — это контейнер для данных, значение которого НЕ может изменяться после присвоения.

```swift
let name = "Алексей"
print(name)  // Алексей

// name = "Иван"  // ❌ ОШИБКА! Константу нельзя изменить
```

### Когда использовать var, а когда let?

> **Правило**: Всегда используйте `let` по умолчанию. Используйте `var` только когда значение действительно будет меняться.

Xcode подскажет, если вы используете `var` там, где можно использовать `let`.

```swift
// ✅ Хорошо: значение не меняется — используем let
let maximumAttempts = 3
let welcomeMessage = "Добро пожаловать!"
let pi = 3.14159

// ✅ Хорошо: значение меняется — используем var
var currentAttempt = 0
currentAttempt = 1
currentAttempt = 2

var totalScore = 0
totalScore += 100
```

### Сравнение с JavaScript

```javascript
// JavaScript
const name = "Алексей";  // константа
let score = 0;           // переменная (может меняться)
var oldStyle = "avoid";  // старый стиль (избегайте)
```

```swift
// Swift
let name = "Алексей"     // константа
var score = 0            // переменная
// В Swift нет "const" — используется "let"
```

### Именование переменных

В Swift используется **camelCase** (как в JavaScript):

```swift
// ✅ Правильно
let firstName = "Иван"
let lastName = "Петров"
let numberOfItems = 5
let isLoggedIn = true

// ❌ Неправильно
let first_name = "Иван"    // snake_case не принят
let FirstName = "Иван"     // PascalCase для типов, не переменных
let n = 5                   // слишком короткое, непонятное имя
```

---

## 2.3 Базовые типы данных

Swift — это **строго типизированный** язык. Это значит, что каждая переменная имеет определённый тип, и этот тип не может измениться.

### Int (целые числа)

```swift
let age: Int = 25
let year: Int = 2024
let negativeNumber: Int = -100
let million: Int = 1_000_000  // подчёркивания для читаемости

// Операции с целыми числами
let sum = 10 + 5        // 15
let difference = 10 - 5  // 5
let product = 10 * 5     // 50
let quotient = 10 / 3    // 3 (целочисленное деление!)
let remainder = 10 % 3   // 1 (остаток от деления)
```

> **Важно**: При делении двух целых чисел результат тоже целое число (дробная часть отбрасывается).

### Double и Float (числа с плавающей точкой)

```swift
let price: Double = 19.99
let temperature: Double = -5.5
let pi: Double = 3.14159265359

// Float менее точный, но занимает меньше памяти
let floatNumber: Float = 3.14

// Операции с дробными числами
let total = 10.0 / 3.0   // 3.333333...
let rounded = 10.5 + 0.5  // 11.0
```

> **Совет**: Используйте `Double` по умолчанию. `Float` нужен редко.

### String (строки)

```swift
let greeting: String = "Привет!"
let name: String = "Мир"
let empty: String = ""

// Многострочные строки
let poem: String = """
    Это многострочная строка.
    Она может содержать несколько строк.
    Очень удобно для длинного текста.
    """

// Операции со строками
let fullGreeting = greeting + " " + name  // "Привет! Мир"
let count = greeting.count                 // 7 (количество символов)
let isEmpty = empty.isEmpty                // true
let upper = greeting.uppercased()          // "ПРИВЕТ!"
let lower = greeting.lowercased()          // "привет!"
```

### Bool (логический тип)

```swift
let isActive: Bool = true
let isCompleted: Bool = false

// Логические операции
let and = true && false   // false (И)
let or = true || false    // true (ИЛИ)
let not = !true           // false (НЕ)

// Сравнения возвращают Bool
let isEqual = (5 == 5)      // true
let isNotEqual = (5 != 3)   // true
let isGreater = (10 > 5)    // true
let isLessOrEqual = (5 <= 5) // true
```

### Character (одиночный символ)

```swift
let letter: Character = "A"
let emoji: Character = "🍎"
let digit: Character = "7"

// Character можно использовать в строках
let word: String = "Swift"
let firstChar: Character = word.first!  // "S"
```

---

## 2.4 Строки и интерполяция

### Интерполяция строк

**Интерполяция** — это вставка значений переменных внутрь строки.

```swift
let name = "Анна"
let age = 28

// Интерполяция с \()
let message = "Меня зовут \(name), мне \(age) лет."
print(message)  // "Меня зовут Анна, мне 28 лет."

// Можно вставлять выражения
let price = 100.0
let tax = 0.2
let total = "Итого: \(price * (1 + tax)) руб."
print(total)  // "Итого: 120.0 руб."
```

### Сравнение с JavaScript

```javascript
// JavaScript — шаблонные строки
const message = `Меня зовут ${name}, мне ${age} лет.`;
```

```swift
// Swift — интерполяция
let message = "Меня зовут \(name), мне \(age) лет."
```

### Специальные символы

```swift
let newLine = "Первая строка\nВторая строка"
let tab = "Колонка1\tКолонка2"
let quote = "Он сказал: \"Привет!\""
let backslash = "Путь: C:\\Users\\Name"

print(newLine)
// Первая строка
// Вторая строка
```

### Полезные методы строк

```swift
let text = "  Hello, Swift!  "

// Удаление пробелов по краям
let trimmed = text.trimmingCharacters(in: .whitespaces)
// "Hello, Swift!"

// Проверка содержимого
let hasHello = text.contains("Hello")  // true
let hasWorld = text.contains("World")  // false

// Разделение строки
let parts = "apple,banana,orange".split(separator: ",")
// ["apple", "banana", "orange"]

// Замена
let replaced = "Hello, World!".replacingOccurrences(of: "World", with: "Swift")
// "Hello, Swift!"

// Проверка начала и конца
let startsWithHello = "Hello, World!".hasPrefix("Hello")  // true
let endsWithMark = "Hello, World!".hasSuffix("!")         // true
```

---

## 2.5 Приведение типов

Иногда нужно преобразовать один тип в другой.

### Числовые преобразования

```swift
let intNumber: Int = 42
let doubleNumber: Double = Double(intNumber)  // 42.0

let pi: Double = 3.14159
let roundedPi: Int = Int(pi)  // 3 (дробная часть отбрасывается)

let floatValue: Float = 3.14
let doubleValue: Double = Double(floatValue)  // 3.14
```

> **Важно**: В Swift нельзя автоматически смешивать типы в операциях!

```swift
let a: Int = 5
let b: Double = 2.5

// let result = a + b  // ❌ ОШИБКА! Разные типы

let result1 = Double(a) + b  // ✅ 7.5 (преобразовали Int в Double)
let result2 = a + Int(b)     // ✅ 7 (преобразовали Double в Int)
```

### Строковые преобразования

```swift
// Число в строку
let number = 42
let numberString = String(number)  // "42"

let price = 19.99
let priceString = String(price)    // "19.99"

// Строка в число (возвращает Optional!)
let text = "123"
let parsed = Int(text)      // Optional(123)

let invalid = "hello"
let failed = Int(invalid)   // nil (невозможно преобразовать)
```

> **Примечание**: Преобразование строки в число возвращает **Optional**, потому что преобразование может не удаться. Об Optional мы поговорим в Главе 7.

### Bool преобразования

```swift
// В Swift нет автоматического преобразования в Bool
let number = 1

// if number { }  // ❌ ОШИБКА в Swift!
if number != 0 { }  // ✅ Нужно явное сравнение

// Строка в Bool
let trueString = "true"
let boolValue = Bool(trueString)  // Optional(true)
```

---

## 2.6 Type Inference (Вывод типов)

Swift может автоматически определить тип переменной по присвоенному значению.

```swift
// Явное указание типа
let explicitInt: Int = 42
let explicitString: String = "Hello"
let explicitBool: Bool = true

// Type Inference — тип определяется автоматически
let inferredInt = 42           // Swift понимает, что это Int
let inferredDouble = 3.14      // Swift понимает, что это Double
let inferredString = "Hello"   // Swift понимает, что это String
let inferredBool = true        // Swift понимает, что это Bool
```

### Когда указывать тип явно?

```swift
// ✅ Когда тип неочевиден из значения
let amount: Double = 100  // без указания типа это был бы Int

// ✅ Когда нет начального значения
let name: String  // объявили, но не присвоили
// ... позже в коде
name = "Алексей"

// ✅ Для документирования кода
let userId: Int = fetchUserId()  // понятно, что функция возвращает Int
```

### Проверка типа

```swift
let value = 42

// Проверка типа во время выполнения
print(type(of: value))  // Int

// is — проверяет, принадлежит ли значение типу
if value is Int {
    print("Это целое число")
}
```

---

## 2.7 Комментарии

Комментарии помогают объяснить код. Они игнорируются компилятором.

### Однострочные комментарии

```swift
// Это однострочный комментарий
let name = "Алексей"  // Комментарий в конце строки
```

### Многострочные комментарии

```swift
/*
 Это многострочный комментарий.
 Он может занимать несколько строк.
 Полезен для больших блоков текста.
*/
let value = 42

/* Многострочные комментарии можно вкладывать
   /* вот так */
   в отличие от многих других языков
*/
```

### Документирующие комментарии

```swift
/// Вычисляет площадь прямоугольника.
/// - Parameters:
///   - width: Ширина прямоугольника
///   - height: Высота прямоугольника
/// - Returns: Площадь прямоугольника
func calculateArea(width: Double, height: Double) -> Double {
    return width * height
}
```

Такие комментарии показываются в Quick Help (Option + клик на функцию).

### MARK, TODO, FIXME

Специальные комментарии для организации кода:

```swift
// MARK: - Свойства
let name = "Алексей"
let age = 25

// MARK: - Методы
func greet() {
    print("Привет!")
}

// TODO: Добавить валидацию email
func validateEmail() {
    // ...
}

// FIXME: Исправить баг с отрицательными числами
func calculate() {
    // ...
}
```

`MARK` создаёт разделители в меню навигации Xcode (`Ctrl + 6`).

---

## 2.8 Операторы

### Арифметические операторы

```swift
let a = 10
let b = 3

let sum = a + b         // 13 (сложение)
let difference = a - b  // 7 (вычитание)
let product = a * b     // 30 (умножение)
let quotient = a / b    // 3 (целочисленное деление)
let remainder = a % b   // 1 (остаток от деления)

// Унарный минус
let negative = -a       // -10
```

### Составные операторы присваивания

```swift
var score = 0

score += 10  // score = score + 10 → 10
score -= 3   // score = score - 3 → 7
score *= 2   // score = score * 2 → 14
score /= 2   // score = score / 2 → 7
score %= 3   // score = score % 3 → 1
```

### Операторы сравнения

```swift
let x = 5
let y = 10

x == y   // false (равно)
x != y   // true (не равно)
x > y    // false (больше)
x < y    // true (меньше)
x >= y   // false (больше или равно)
x <= y   // true (меньше или равно)
```

### Логические операторы

```swift
let isRaining = true
let hasUmbrella = false

// И (AND) — оба должны быть true
let shouldGoOut = isRaining && hasUmbrella  // false

// ИЛИ (OR) — хотя бы один должен быть true
let stayHome = isRaining || !hasUmbrella    // true

// НЕ (NOT) — инверсия
let isSunny = !isRaining                     // false
```

### Оператор тернарного условия

```swift
let age = 20
let status = age >= 18 ? "взрослый" : "несовершеннолетний"
print(status)  // "взрослый"

// Эквивалентно:
// if age >= 18 {
//     status = "взрослый"
// } else {
//     status = "несовершеннолетний"
// }
```

---

## 2.9 Практические примеры

### Пример 1: Калькулятор скидки

```swift
let originalPrice = 1000.0
let discountPercent = 20.0

let discountAmount = originalPrice * discountPercent / 100
let finalPrice = originalPrice - discountAmount

print("Оригинальная цена: \(originalPrice) руб.")
print("Скидка: \(discountPercent)% = \(discountAmount) руб.")
print("Итоговая цена: \(finalPrice) руб.")
```

### Пример 2: Конвертер температуры

```swift
let celsius = 25.0

// Формула: F = C × 9/5 + 32
let fahrenheit = celsius * 9 / 5 + 32

print("\(celsius)°C = \(fahrenheit)°F")
// 25.0°C = 77.0°F
```

### Пример 3: Форматирование имени

```swift
let firstName = "иван"
let lastName = "ПЕТРОВ"

// Приводим к нужному формату
let formattedFirst = firstName.capitalized  // "Иван"
let formattedLast = lastName.capitalized    // "Петров"

let fullName = "\(formattedFirst) \(formattedLast)"
print(fullName)  // "Иван Петров"
```

### Пример 4: Проверка возраста

```swift
let birthYear = 1995
let currentYear = 2024

let age = currentYear - birthYear
let isAdult = age >= 18

let message = isAdult
    ? "Вам \(age) лет. Доступ разрешён."
    : "Вам \(age) лет. Доступ запрещён."

print(message)
```

---

## 2.10 Упражнения

### Упражнение 2.1: Переменные и константы

Создайте Playground и объявите:
1. Константу `myName` с вашим именем
2. Константу `birthYear` с годом вашего рождения
3. Переменную `currentYear` со значением 2024
4. Вычислите ваш возраст и выведите сообщение:
   "Меня зовут [имя], мне [возраст] лет."

```swift
// Ваш код здесь
```

<details>
<summary>Показать решение</summary>

```swift
let myName = "Алексей"
let birthYear = 1995
var currentYear = 2024

let age = currentYear - birthYear
print("Меня зовут \(myName), мне \(age) лет.")
```
</details>

---

### Упражнение 2.2: Типы данных

Определите тип каждого значения и исправьте ошибки:

```swift
let a = 42
let b = 3.14
let c = "Hello"
let d = true
let e = "X"

// Какой тип у каждой переменной?
// a: ???
// b: ???
// c: ???
// d: ???
// e: ???
```

<details>
<summary>Показать решение</summary>

```swift
let a = 42        // Int
let b = 3.14      // Double
let c = "Hello"   // String
let d = true      // Bool
let e = "X"       // String (не Character, т.к. не указан тип явно)

// Чтобы e был Character:
let e: Character = "X"
```
</details>

---

### Упражнение 2.3: Интерполяция строк

Создайте карточку товара:

```swift
let productName = "iPhone 15 Pro"
let price = 999.99
let inStock = true
let quantity = 5

// Выведите:
// Товар: iPhone 15 Pro
// Цена: $999.99
// В наличии: Да
// Количество: 5 шт.
```

<details>
<summary>Показать решение</summary>

```swift
let productName = "iPhone 15 Pro"
let price = 999.99
let inStock = true
let quantity = 5

let availability = inStock ? "Да" : "Нет"

print("Товар: \(productName)")
print("Цена: $\(price)")
print("В наличии: \(availability)")
print("Количество: \(quantity) шт.")
```
</details>

---

### Упражнение 2.4: Приведение типов

Исправьте код, чтобы он компилировался:

```swift
let width: Int = 10
let height: Double = 5.5

// let area = width * height  // Ошибка!
// Исправьте и вычислите площадь
```

<details>
<summary>Показать решение</summary>

```swift
let width: Int = 10
let height: Double = 5.5

let area = Double(width) * height  // 55.0
print("Площадь: \(area)")
```
</details>

---

### Упражнение 2.5: Калькулятор BMI

Напишите программу для расчёта индекса массы тела (BMI):

Формула: BMI = вес (кг) / рост (м)²

```swift
let weightKg = 75.0
let heightCm = 180.0

// 1. Переведите рост из см в метры
// 2. Вычислите BMI
// 3. Выведите результат с одним знаком после запятой
```

<details>
<summary>Показать решение</summary>

```swift
let weightKg = 75.0
let heightCm = 180.0

let heightM = heightCm / 100  // 1.8
let bmi = weightKg / (heightM * heightM)  // 23.148...

// Округление до одного знака
let roundedBmi = round(bmi * 10) / 10  // 23.1

print("Ваш BMI: \(roundedBmi)")
```
</details>

---

### Упражнение 2.6: Конвертер валют

Создайте конвертер долларов в рубли:

```swift
let usdAmount = 100.0
let exchangeRate = 92.5  // курс: 1 USD = 92.5 RUB

// Конвертируйте и выведите результат
```

<details>
<summary>Показать решение</summary>

```swift
let usdAmount = 100.0
let exchangeRate = 92.5

let rubAmount = usdAmount * exchangeRate

print("\(usdAmount) USD = \(rubAmount) RUB")
// 100.0 USD = 9250.0 RUB
```
</details>

---

### Упражнение 2.7: Работа со строками

Дана строка с email. Извлеките имя пользователя и домен:

```swift
let email = "user@example.com"

// Выведите:
// Пользователь: user
// Домен: example.com
```

<details>
<summary>Показать решение</summary>

```swift
let email = "user@example.com"

let parts = email.split(separator: "@")
let username = String(parts[0])
let domain = String(parts[1])

print("Пользователь: \(username)")
print("Домен: \(domain)")
```
</details>

---

## Итоги главы

В этой главе вы узнали:

✅ Разницу между `var` (переменные) и `let` (константы)

✅ Основные типы данных: `Int`, `Double`, `String`, `Bool`, `Character`

✅ Как работает интерполяция строк с `\()`

✅ Правила приведения типов в Swift

✅ Что такое Type Inference и когда указывать тип явно

✅ Виды комментариев и специальные метки (MARK, TODO, FIXME)

✅ Арифметические, сравнительные и логические операторы

---

## Следующая глава

В [Главе 3](../03-Control-Flow/README.md) мы изучим управление потоком выполнения: условные операторы, циклы и коллекции.

---

> **Совет**: Прежде чем переходить дальше, убедитесь, что вы можете без подсказок:
> - Объявить переменную и константу
> - Использовать все базовые типы данных
> - Создать строку с интерполяцией
> - Преобразовать типы друг в друга
