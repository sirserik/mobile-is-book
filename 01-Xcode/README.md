# Глава 1: Знакомство с iOS разработкой и Xcode

## Введение

Добро пожаловать в мир iOS разработки! В этой главе вы познакомитесь с основным инструментом каждого iOS разработчика — Xcode. Если вы работали с веб-разработкой, то Xcode можно сравнить с VS Code или WebStorm, но со встроенной поддержкой всего, что нужно для создания приложений под устройства Apple.

---

## 1.1 Что такое iOS разработка

### Экосистема Apple

iOS — это операционная система, работающая на iPhone и iPod Touch. Apple также имеет другие платформы:

- **iPadOS** — для iPad (очень похож на iOS)
- **watchOS** — для Apple Watch
- **tvOS** — для Apple TV
- **macOS** — для компьютеров Mac

Хорошая новость: выучив iOS разработку, вы сможете создавать приложения для всех этих платформ!

### Языки программирования

Для iOS разработки используются два языка:

1. **Swift** — современный язык, созданный Apple в 2014 году. Это основной язык для новых проектов.
2. **Objective-C** — старый язык, использовавшийся до Swift. Встречается в legacy-проектах.

В этой книге мы будем использовать **только Swift** — он проще, безопаснее и современнее.

### Сравнение с веб-разработкой

| Веб-разработка | iOS разработка |
|----------------|----------------|
| Браузер выполняет код | iOS выполняет код |
| HTML определяет структуру | UIKit/SwiftUI определяют интерфейс |
| CSS стилизует элементы | Свойства UIView стилизуют элементы |
| JavaScript добавляет логику | Swift добавляет логику |
| Chrome DevTools для отладки | Xcode Debugger для отладки |
| npm для пакетов | Swift Package Manager для пакетов |

---

## 1.2 Установка Xcode

### Системные требования

Перед установкой убедитесь, что ваш Mac соответствует требованиям:

- **macOS**: Monterey 12.0 или новее
- **Память**: минимум 8 ГБ RAM (рекомендуется 16 ГБ)
- **Диск**: минимум 35 ГБ свободного места
- **Процессор**: любой Mac с чипом Apple Silicon (M1, M2, M3) или Intel

### Установка из App Store

1. Откройте **App Store** на вашем Mac
2. В поиске введите **"Xcode"**
3. Нажмите **"Загрузить"** (или "Установить")
4. Дождитесь завершения загрузки (~12 ГБ)
5. После установки откройте Xcode из папки Applications

> **Важно**: Первый запуск Xcode может занять несколько минут — он установит дополнительные компоненты.

### Принятие лицензии

При первом запуске Xcode попросит:
1. Принять лицензионное соглашение
2. Ввести пароль администратора для установки компонентов

### Установка Command Line Tools

Command Line Tools — это набор инструментов для работы с Swift из терминала. Обычно они устанавливаются автоматически с Xcode, но можно проверить:

```bash
# Откройте Terminal и выполните:
xcode-select --install
```

Если инструменты уже установлены, вы увидите сообщение об этом.

---

## 1.3 Интерфейс Xcode

При первом открытии Xcode вы увидите окно приветствия. Давайте разберём основные области интерфейса.

### Окно приветствия (Welcome Window)

При запуске Xcode показывает окно с опциями:

- **Create a new Xcode project** — создать новый проект
- **Clone an existing project** — клонировать проект из Git
- **Open a project or file** — открыть существующий проект
- **Recent projects** — список недавних проектов

### Основной интерфейс

После открытия проекта вы увидите основной интерфейс с несколькими областями:

```
┌─────────────────────────────────────────────────────────────────┐
│                        Toolbar (Панель инструментов)            │
├──────────┬──────────────────────────────────────┬───────────────┤
│          │                                      │               │
│ Navigator│           Editor Area                │   Inspector   │
│  (Левая  │        (Центральная область          │   (Правая     │
│  панель) │         редактирования)              │   панель)     │
│          │                                      │               │
│          │                                      │               │
├──────────┴──────────────────────────────────────┴───────────────┤
│                    Debug Area (Область отладки)                  │
└─────────────────────────────────────────────────────────────────┘
```

### 1. Toolbar (Панель инструментов)

Расположена вверху окна:

- **Run button (▶️)** — запустить приложение
- **Stop button (⏹)** — остановить приложение
- **Scheme selector** — выбор устройства для запуска (симулятор или реальное устройство)
- **Status bar** — показывает текущий статус (сборка, ошибки и т.д.)

### 2. Navigator (Левая панель)

Показывает структуру проекта. Есть несколько вкладок:

| Иконка | Название | Описание |
|--------|----------|----------|
| 📁 | Project Navigator | Файлы проекта (как проводник) |
| 🔍 | Search Navigator | Поиск по проекту |
| ⚠️ | Issue Navigator | Ошибки и предупреждения |
| 🧪 | Test Navigator | Тесты |
| 🐛 | Debug Navigator | Информация об отладке |
| 📊 | Report Navigator | Отчёты о сборке |

**Горячая клавиша**: `Cmd + 0` — скрыть/показать Navigator

### 3. Editor Area (Область редактирования)

Центральная часть — здесь вы пишете код. Может отображать:

- **Source Editor** — редактор кода
- **Interface Builder** — визуальный редактор интерфейса
- **Asset Catalog** — менеджер ресурсов (картинки, иконки)
- **Property List Editor** — редактор plist файлов

### 4. Inspector (Правая панель)

Показывает свойства выбранного элемента:

- **File Inspector** — информация о файле
- **Quick Help** — документация
- **Identity Inspector** — идентификатор элемента UI
- **Attributes Inspector** — свойства элемента
- **Size Inspector** — размеры и позиция

**Горячая клавиша**: `Cmd + Option + 0` — скрыть/показать Inspector

### 5. Debug Area (Область отладки)

Расположена внизу, появляется при запуске приложения:

- **Variables View** — переменные в текущем контексте
- **Console** — вывод логов и команд отладки

**Горячая клавиша**: `Cmd + Shift + Y` — скрыть/показать Debug Area

---

## 1.4 Создание первого проекта

Давайте создадим ваш первый iOS проект!

### Шаг 1: Создание проекта

1. Откройте Xcode
2. Выберите **"Create a new Xcode project"**
3. Выберите **iOS** → **App**
4. Нажмите **Next**

### Шаг 2: Настройка проекта

Заполните поля:

| Поле | Значение | Описание |
|------|----------|----------|
| **Product Name** | MyFirstApp | Название приложения |
| **Team** | None (или ваш Apple ID) | Команда разработчика |
| **Organization Identifier** | com.yourname | Уникальный идентификатор (как домен наоборот) |
| **Bundle Identifier** | Автоматически | Полный ID приложения |
| **Interface** | **Storyboard** | Способ создания UI |
| **Language** | **Swift** | Язык программирования |

> **Важно**: Выберите **Storyboard**, а не SwiftUI. В этой книге мы используем UIKit.

### Шаг 3: Сохранение проекта

1. Нажмите **Next**
2. Выберите папку для сохранения (например, Documents)
3. Убедитесь, что **Create Git repository** отмечено
4. Нажмите **Create**

### Структура проекта

После создания вы увидите следующие файлы:

```
MyFirstApp/
├── MyFirstApp.xcodeproj     # Файл проекта Xcode
├── MyFirstApp/
│   ├── AppDelegate.swift    # Точка входа приложения
│   ├── SceneDelegate.swift  # Управление сценами (окнами)
│   ├── ViewController.swift # Контроллер главного экрана
│   ├── Main.storyboard      # Визуальный интерфейс
│   ├── Assets.xcassets      # Ресурсы (картинки, иконки)
│   ├── LaunchScreen.storyboard # Экран загрузки
│   └── Info.plist           # Настройки приложения
└── MyFirstAppTests/         # Юнит-тесты
```

### Сравнение со структурой веб-проекта

| iOS файл | Аналог в веб | Назначение |
|----------|--------------|------------|
| AppDelegate.swift | index.js (точка входа) | Инициализация приложения |
| ViewController.swift | Компонент React/Vue | Логика экрана |
| Main.storyboard | HTML файл | Структура интерфейса |
| Assets.xcassets | Папка images/ | Ресурсы |
| Info.plist | package.json / config | Настройки |

---

## 1.5 Симулятор iOS

Симулятор позволяет запускать приложения без реального iPhone.

### Запуск приложения

1. В Toolbar выберите симулятор (например, **iPhone 15 Pro**)
2. Нажмите кнопку **Run (▶️)** или `Cmd + R`
3. Дождитесь запуска симулятора
4. Ваше приложение откроется автоматически

### Управление симулятором

| Действие | Как сделать |
|----------|-------------|
| Нажатие на экран | Клик мышью |
| Свайп | Зажать и провести мышью |
| Домой | `Cmd + Shift + H` |
| Блокировка экрана | `Cmd + L` |
| Поворот экрана | `Cmd + ←` или `Cmd + →` |
| Скриншот | `Cmd + S` |
| Сброс симулятора | Device → Erase All Content and Settings |

### Меню симулятора

- **Device** — управление устройством
- **I/O** — имитация сенсоров (GPS, Face ID)
- **Features** — дополнительные функции

### Выбор разных устройств

В Xcode можно выбрать разные симуляторы:
- iPhone SE (маленький экран)
- iPhone 15 (стандартный)
- iPhone 15 Pro Max (большой экран)
- iPad (планшет)

> **Совет**: Тестируйте на разных размерах экрана, чтобы убедиться, что интерфейс выглядит хорошо везде.

---

## 1.6 Playground — песочница для экспериментов

Playground — это интерактивная среда для изучения Swift без создания полноценного проекта. Идеально подходит для обучения!

### Создание Playground

1. **File** → **New** → **Playground** (или `Cmd + Shift + Option + N`)
2. Выберите **Blank** в разделе iOS
3. Нажмите **Next**
4. Введите имя (например, "SwiftPractice") и сохраните

### Интерфейс Playground

```
┌────────────────────────────────────────────────────────────────┐
│  Код Swift                              │  Результаты          │
│                                         │                      │
│  let name = "Мир"                       │  "Мир"               │
│  print("Привет, \(name)!")              │  "Привет, Мир!"      │
│                                         │                      │
│  let sum = 2 + 2                        │  4                   │
│                                         │                      │
└────────────────────────────────────────────────────────────────┘
```

Слева вы пишете код, справа сразу видите результат!

### Пример использования

Напишите в Playground:

```swift
import UIKit

// Это ваш первый код на Swift!
let greeting = "Привет"
let name = "Swift"
let message = "\(greeting), \(name)!"

print(message)

// Простые вычисления
let a = 10
let b = 20
let sum = a + b

// Результат будет виден справа
```

### Выполнение кода

- **Автоматически**: код выполняется по мере ввода
- **Вручную**: нажмите кнопку ▶️ слева от строки или `Cmd + Shift + Enter`

### Когда использовать Playground

✅ **Используйте Playground для:**
- Изучения синтаксиса Swift
- Экспериментов с кодом
- Тестирования алгоритмов
- Выполнения упражнений из этой книги

❌ **Не используйте для:**
- Создания реальных приложений
- Работы с UI (есть ограничения)

---

## 1.7 Отладка кода

Отладка (debugging) — это процесс поиска и исправления ошибок в коде.

### Виды ошибок

1. **Compile-time errors** — ошибки компиляции (код не соберётся)
2. **Runtime errors** — ошибки выполнения (приложение упадёт)
3. **Logic errors** — логические ошибки (приложение работает неправильно)

### Breakpoints (Точки останова)

Breakpoint позволяет остановить выполнение программы на определённой строке.

**Как поставить breakpoint:**
1. Кликните на номер строки в левом поле редактора
2. Появится синий маркер — это breakpoint

**Как убрать breakpoint:**
- Кликните на него ещё раз и перетащите в сторону

### Использование Debugger

Когда программа остановится на breakpoint:

1. **Continue (▶️)** — продолжить выполнение
2. **Step Over (⤵️)** — выполнить строку и перейти к следующей
3. **Step Into (↓)** — войти внутрь функции
4. **Step Out (↑)** — выйти из функции

**Горячие клавиши:**
- `Cmd + Y` — включить/выключить breakpoints
- `F6` — Step Over
- `F7` — Step Into
- `F8` — Step Out

### Консоль отладки

В Debug Area → Console вы можете:

```swift
// В коде используйте print для вывода в консоль
print("Значение переменной x: \(x)")

// Можно выводить несколько значений
print("a =", a, "b =", b, "sum =", a + b)
```

### LLDB команды

В консоли можно вводить команды отладчика:

```
(lldb) po variableName       // Напечатать значение переменной
(lldb) p variableName        // То же, с типом
(lldb) expression x = 5      // Изменить значение во время отладки
```

### View Hierarchy Debugger

Для отладки интерфейса:

1. Запустите приложение
2. Нажмите **Debug** → **View Debugging** → **Capture View Hierarchy**
3. Увидите 3D-представление всех слоёв интерфейса

Это помогает понять, как элементы UI расположены друг относительно друга.

---

## 1.8 Горячие клавиши Xcode

Запомните эти горячие клавиши — они сильно ускоряют работу:

### Основные

| Клавиши | Действие |
|---------|----------|
| `Cmd + R` | Запустить приложение |
| `Cmd + .` | Остановить приложение |
| `Cmd + B` | Собрать проект (без запуска) |
| `Cmd + Shift + K` | Очистить проект |

### Навигация

| Клавиши | Действие |
|---------|----------|
| `Cmd + Shift + O` | Быстрое открытие файла |
| `Cmd + Shift + J` | Показать файл в Navigator |
| `Ctrl + 6` | Список методов в файле |
| `Cmd + клик` | Перейти к определению |

### Редактирование

| Клавиши | Действие |
|---------|----------|
| `Cmd + /` | Закомментировать строку |
| `Cmd + [` | Уменьшить отступ |
| `Cmd + ]` | Увеличить отступ |
| `Ctrl + I` | Автоформатирование |
| `Cmd + Option + [` | Переместить строку вверх |
| `Cmd + Option + ]` | Переместить строку вниз |

### Панели

| Клавиши | Действие |
|---------|----------|
| `Cmd + 0` | Скрыть/показать Navigator |
| `Cmd + Option + 0` | Скрыть/показать Inspector |
| `Cmd + Shift + Y` | Скрыть/показать Debug Area |

---

## 1.9 Упражнения

### Упражнение 1.1: Создайте проект
1. Создайте новый iOS проект с названием "HelloWorld"
2. Выберите Storyboard и Swift
3. Запустите его на симуляторе iPhone 15

### Упражнение 1.2: Исследуйте интерфейс
1. Найдите все панели Xcode (Navigator, Editor, Inspector, Debug Area)
2. Попробуйте скрыть и показать каждую панель горячими клавишами
3. Откройте каждый файл проекта и посмотрите его содержимое

### Упражнение 1.3: Playground
1. Создайте новый Playground
2. Напишите код:
```swift
let myName = "Ваше имя"
let myAge = 25 // ваш возраст
print("Меня зовут \(myName), мне \(myAge) лет")

let nextYear = myAge + 1
print("В следующем году мне будет \(nextYear)")
```
3. Убедитесь, что результаты отображаются справа

### Упражнение 1.4: Отладка
1. В Playground добавьте breakpoint на любую строку
2. Запустите код и посмотрите, как он остановится
3. Используйте Step Over для пошагового выполнения
4. Посмотрите значения переменных в Debug Area

### Упражнение 1.5: Горячие клавиши
Попрактикуйтесь с горячими клавишами:
1. `Cmd + Shift + O` — откройте файл ViewController.swift
2. `Cmd + /` — закомментируйте строку
3. `Ctrl + I` — отформатируйте код
4. `Cmd + B` — соберите проект

---

## Итоги главы

В этой главе вы узнали:

✅ Что такое iOS разработка и как она связана с веб-разработкой

✅ Как установить Xcode

✅ Основные области интерфейса Xcode

✅ Как создать первый iOS проект

✅ Как использовать симулятор

✅ Что такое Playground и как его использовать для обучения

✅ Основы отладки кода

✅ Важные горячие клавиши

---

## Следующая глава

В [Главе 2](../02-Swift-Basics/README.md) мы начнём изучать язык Swift — переменные, константы и типы данных.

---

> **Совет**: Не торопитесь переходить к следующей главе. Убедитесь, что вы комфортно чувствуете себя в Xcode. Попробуйте поиграть с Playground, создать несколько проектов, понажимать разные кнопки. Это ваш основной инструмент на ближайшее время!
